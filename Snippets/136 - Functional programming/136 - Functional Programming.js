const myFunciton = e => e * 2;

const arr = [1, 3, 8, 2];

const squared = arr.map(e => e * e);
// [1, 9, 64, 4]

const doubled = arr.map(e => e * 2);
// [2, 6, 16, 4]

const plusFive = arr.map(e => e + 5);
// [6, 8, 13, 7]